/**
 * Reusable grid of image thumbnails. Currently inlined in pick-frame/pick-room.
 * Kept as a placeholder for future deduplication.
 */
export function mountThumbGrid(/* container, items, onSelect */) {
  throw new Error('thumb-grid extracted helper not yet implemented');
}
